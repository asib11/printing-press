
// eslint-disable-next-line react/prop-types
const GiftingImage = ({image}) => {
    return (
        <div>
           <img src={image} alt="..." className="rounded-lg h-full"/> 
        </div>
    );
};

export default GiftingImage;